version: "3"

services:
  redis:
    image: "redis:latest"
    ports: 
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=password
      - REDIS_USER=root
      - REDIS_USER_PASSWORD=password
    restart:
      always